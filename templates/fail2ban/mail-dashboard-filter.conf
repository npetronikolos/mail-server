# Managed by mail-server install.sh
# Failed sign-ins to the admin dashboard and to calendar/contacts.
[Definition]
failregex = ^\S+ (?:LOGIN FAILED|2FA FAILED|DAV FAILED|PASSWORD CHANGE FAILED) ip=<HOST> 
ignoreregex =
datepattern = ^%%Y-%%m-%%dT%%H:%%M:%%S
