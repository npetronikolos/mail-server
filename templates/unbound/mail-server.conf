# Managed by mail-server install.sh
# Local recursive DNS resolver used by Rspamd for blocklist lookups.
server:
  interface: 127.0.0.1
  port: 53
  access-control: 127.0.0.0/8 allow
  do-ip6: {{UNBOUND_IPV6}}
  hide-identity: yes
  hide-version: yes
  prefetch: yes
