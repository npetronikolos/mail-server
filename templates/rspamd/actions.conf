# Managed by mail-server install.sh
# Scores at which Rspamd acts on incoming mail.
reject = 15;       # refuse the message
add_header = 6;    # deliver, but tag as spam (Sieve moves it to Junk)
greylist = 4;      # ask the sender to retry later
